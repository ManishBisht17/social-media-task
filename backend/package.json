{
  "name": "social-media-task",
  "version": "1.0.0",
  "description": "Social media application",
  "main": "index.js",
  "scripts": {
    "start": "node backend/server.js",
    "build": "mkdir -p build && cp -r frontend/* build/"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "mongoose": "^7.0.3",
    "multer": "^1.4.5-lts.1"
  }
}